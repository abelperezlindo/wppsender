const config = {
    database: {
        host: 'localhost',
        port: '49175',
        user: "db",
        password: "db",
        database: "db",
        multipleStatements: true
    }
}

module.exports = config;