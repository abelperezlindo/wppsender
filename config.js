const config = {
    database: {
        host: 'localhost',
        port: '',
        user: "admin",
        password: "admin",
        database: "wppsender",
        multipleStatements: true
    }
}

module.exports = config;