const config = {
    database: {
        host: 'localhost',
        user: "admin",
        password: "admin",
        database: "wppsender",
        multipleStatements: true
    }
}

module.exports = config;