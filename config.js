const config = {
    database: {
        host: 'localhost',
        port: '49163',
        user: "db",
        password: "db",
        database: "db",
        multipleStatements: true
    }
}

module.exports = config;